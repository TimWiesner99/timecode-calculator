[project]
name = "timecode-calculator"
version = "0.1.0"
description = "A web-based tool for performing calculations with timecodes in hh:mm:ss:ff format"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "Flask>=3.0.0",
]

[project.scripts]
timecode-calculator = "app:main"
